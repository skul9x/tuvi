# Audit Report - 2026-01-30

## Summary
- 🔴 Critical Issues: 1
- 🟡 Warnings: 1
- 🟢 Suggestions: 2

## 🔴 Critical Issues (Phải sửa ngay)
1.  **Thiếu Validation cho API Chat**
    - **File**: `web/api/chat.py`
    - **Vấn đề**: API này nhận trực tiếp `data_json` từ client mà không kiểm tra kỹ lưỡng. Nếu hacker gửi một file JSON rác hoặc quá lớn, server có thể bị treo.
    - **Cách sửa**: Áp dụng Pydantic `BaseModel` giống như `ansao.py`.

## 🟡 Warnings (Nên xem xét)
1.  **CORS Opening (*)**
    - **File**: `web/api/ansao.py` & `web/api/chat.py`
    - **Vấn đề**: `Access-Control-Allow-Origin: *` cho phép mọi website khác gọi API của anh.
    - **Cách sửa**: Khi nào deploy chính thức, nên đổi `*` thành domain thật của anh (ví dụ: `https://tuvi-huyenbi.vercel.app`).

## 🟢 Suggestions (Tốt hơn nếu làm)
1.  **Rate Limiting**: Hiện tại chưa có giới hạn số lần gọi API. Nếu bị spam, có thể tốn quota Gemini AI. Nên dùng Vercel KV để đếm số request.
2.  **Secret Scrubbing**: Đã quét toàn bộ code và không thấy API Key bị lộ. Rất tốt!

## Next Steps
Anh có muốn em tự động sửa lỗi Critical (thêm Validation cho `chat.py`) luôn không?
